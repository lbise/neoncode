{
  "name": "neoncode-electron-app",
  "version": "0.1.0",
  "private": true,
  "author": "NeonCode contributors",
  "description": "NeonCode Electron app rendering neoncode-hub PTYs with xterm.js.",
  "type": "commonjs",
  "main": "dist/main.js",
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\"",
    "typecheck": "tsc -p tsconfig.node.json --noEmit && tsc -p tsconfig.renderer.json --noEmit && tsc -p tsconfig.tests.json --noEmit",
    "build:node": "tsc -p tsconfig.node.json",
    "build:renderer:modules": "tsc -p tsconfig.renderer.json",
    "build:tests": "tsc -p tsconfig.tests.json",
    "build:renderer": "esbuild renderer.ts --bundle --platform=browser --format=iife --target=es2022 --outfile=dist/renderer.bundle.js",
    "build": "npm run clean && npm run typecheck && npm run build:node && npm run build:renderer:modules && npm run build:tests && npm run build:renderer",
    "test:unit": "node dist/tests/config-store.js && node dist/tests/layout-model.js && node dist/tests/token-loader.js && node dist/tests/hub-manager.js && node dist/tests/hub-client-auth.js && node dist/tests/reconnect-policy.js && node dist/tests/command-catalog.js && node dist/tests/command-registry.js && node dist/tests/command-palette.js && node dist/tests/keybindings.js && node dist/tests/keybinding-router.js && node dist/tests/pane-focus-model.js && node dist/tests/session-model.js && node dist/tests/reconnect-output-soak.js && node dist/tests/terminal-pane-fake-hub.js",
    "check": "npm run build && npm run test:unit",
    "package:alpha": "electron-builder --win --x64 --config electron-builder.yml",
    "start": "electron .",
    "test": "node dist/tests/electron-functional.js"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0"
  },
  "devDependencies": {
    "@electron/fuses": "^2.1.3",
    "@types/node": "24.10.1",
    "electron": "43.1.0",
    "electron-builder": "26.15.3",
    "esbuild-wasm": "0.28.1",
    "playwright": "1.61.1",
    "typescript": "5.9.3"
  }
}
