{
  "name": "neoncode-electron-app",
  "version": "0.1.0",
  "private": true,
  "description": "NeonCode Electron app rendering neoncode-hub PTYs with xterm.js.",
  "main": "main.js",
  "scripts": {
    "build:renderer": "esbuild renderer.js --bundle --platform=browser --format=iife --outfile=renderer.bundle.js",
    "start": "electron .",
    "test": "node tests/electron-functional.js"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0"
  },
  "devDependencies": {
    "electron": "43.1.0",
    "esbuild-wasm": "0.28.1",
    "playwright": "1.61.1"
  }
}
